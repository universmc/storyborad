<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Développement de SmartContrat</title>
    <link rel="stylesheet" href="style.css">
</head>

<body>
    <h1>Développement de SmartContrat</h1>
    <p>Générateur de contenu au format Solidity</p>
    <pre><code class="language-solidity">
/* test_smartContrat.sol */
pragma solidity ^0.8.0;

import "output.sol";

contract TestSmartContrat {
    Output public output;

    constructor(address _address) {
        output = Output(_address);
    }

    function testFunction() public view {
        string memory content = output.generateContent();
        assert(bytes(content).length > 0);
    }
}

    </code></pre>
    <h2>Script Solidity pour le développement du smartContract</h2>
    <pre><code id="smartContractScript"></code></pre>
    <script src="https://cdn.jsdelivr.net/npm/prismjs@1.23.0/components/prism-solidity.min.js"></script>
    <script>
        const solidityScript = `
/* output.sol */
pragma solidity ^0.8.0;

contract Output {
    function generateContent() public pure returns (string memory) {
        return "Ceci est un contenu généré par le smartContrat";
    }
}
        `;
        document.getElementById("smartContractScript").textContent = solidityScript;
        Prism.highlightAll();
    </script>
</body>

</html>
